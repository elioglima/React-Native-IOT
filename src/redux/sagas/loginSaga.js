import { call, put } from 'redux-saga/effects'

export function* loginSaga({ payload }) { // solicita login
    // const retornoAPI = yield call(apiSolicitarLogin, payload)
    // if (retornoAPI.err === true) return yield put(solicitarLoginError(retornoAPI.data))
    // yield put(solicitarLoginSuccess(retornoAPI.data))
}